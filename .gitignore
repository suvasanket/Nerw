# Auto-generated by oz_git
# https://git-scm.com/docs/gitignore `gx` on link for more information.
# Wildcards: * (any chars), ? (single char), [] (char range), ** (any directories)
# Use ! to negate a pattern (re-include files).

.build/
